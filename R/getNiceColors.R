#' Generates vector of n pleasing colors
#'
#' Outputs character vector of n pleasing colors in RGB format
#'
#' @param n number of colors desired in palette
#' @param theme character of the 
#'
#' @return None, saves colors in palette()
#'
#' @examples
#' mycolor <- getNiceColors()
#' colors <- getNiceColors(n=2)
#' plot(c(1,1), type = "h", cex = 5, col = colors)
#'
#' @export
#' 
getNiceColors <- 
function(n = 1, theme = c("taco-salad","apple-orchard","fruit-bowl"))
{
    
}